# setup.py
from setuptools import setup

setup(
    name='nasa_space_explorer',
    version='0.1',
    packages=['nasa_space_explorer'],
    install_requires=[
        'Flask',
        'requests',
    ],
)
